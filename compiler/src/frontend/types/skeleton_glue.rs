use std::collections::{HashMap, HashSet};

use crate::backend::skeleton::ir::{self as s, TypeManager};
use crate::frontend::js::ir as j;
use crate::id::*;

pub fn glue(ir: j::IR, type_manager: &s::TypeManager) -> s::IR {
    todo!()
}
